# 이 파일을 .env 로 복사한 뒤 값을 채우세요(.env 는 공유·커밋 금지).
#
#   cp .env.example .env
#
# 14~22일차·35일차에 쓰던 OpenAI 키를 그대로 씁니다.
# 없으면 준비 셀에서 안내와 함께 멈춥니다.
# 이 단원의 추출 결과는 data/*.jsonl 에 미리 구워 두었으므로 실제 호출은 교안_01 의 시연 몇 건뿐입니다.
# 발급: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-여기에-본인-키
